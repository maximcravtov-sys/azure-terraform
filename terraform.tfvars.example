# Copy this file to terraform.tfvars and fill in your values

resource_group_name = "rg-iis-sql-lb"
location            = "East US"
prefix              = "iis"

vnet_address_space     = "10.0.0.0/16"
subnet_address_prefix  = "10.0.1.0/24"

vm_count = 2
vm_size  = "Standard_DS2_v2"

# Windows Server SKU options:
# - 2022-Datacenter
# - 2019-Datacenter
# - 2016-Datacenter
windows_server_sku = "2022-Datacenter"

# IMPORTANT: Change these values for VMs!
admin_username = "vmadmin"
admin_password = "YourSecurePassword123!"

# IMPORTANT: Change these values for Azure SQL Database!
sql_admin_username = "sqladmin"
sql_admin_password = "YourSecureSQLPassword123!"

# Azure SQL Database configuration
sql_database_name = "appdb"
sql_database_sku  = "S0"  # Options: S0, S1, S2, S3, P1, P2, P4, GP_Gen5_2, etc.
sql_database_max_size_gb = 2
sql_database_zone_redundant = false

tags = {
  Environment = "Production"
  Project     = "IIS-SQL-HA"
  Owner       = "YourName"
}
